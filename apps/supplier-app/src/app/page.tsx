export const metadata = {
  title: 'Megacommerce supplier app',
  description: 'The main Megacommerce.com supplier web app!',
}

// TODO: this page is empty, but its responsibility is to check if the user is authenticated
// and redirect to the dashboard, if not. redirect to login page.
export default function Index() {
  return <div>The Supplier Web App...</div>
}
